apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-text-classification
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-apis.labels" . | nindent 4 }}
    app.kubernetes.io/component: text-classification
  annotations:
    helm.sh/chart: {{ include "ai-apis.chart" . }}
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    {{- if .Values.keda.enabled }}
    nginx.ingress.kubernetes.io/service-upstream: "true"
    {{- end }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.textClassification.ingress.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-text-classification
                port:
                  name: http
  {{- if .Values.textClassification.ingress.tls }}
  tls:
    - hosts:
        - {{ .Values.textClassification.ingress.host | quote }}
      secretName: {{ .Release.Name }}-text-classification-tls
  {{- end }}
{{- if .Values.keda.enabled }}
---
# Internal HTTP-only ingress for bot→textclass calls via KEDA interceptor.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-text-classification-internal
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-apis.labels" . | nindent 4 }}
    app.kubernetes.io/component: text-classification
  annotations:
    helm.sh/chart: {{ include "ai-apis.chart" . }}
    nginx.ingress.kubernetes.io/service-upstream: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.textClassification.ingress.internalHost | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-text-classification
                port:
                  name: http
{{- end }}
