apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-stable-diffusion
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-apis.labels" . | nindent 4 }}
    app.kubernetes.io/component: stable-diffusion
  annotations:
    helm.sh/chart: {{ include "ai-apis.chart" . }}
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    {{- if .Values.keda.enabled }}
    nginx.ingress.kubernetes.io/service-upstream: "true"
    {{- end }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.stableDiffusion.ingress.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-stable-diffusion
                port:
                  name: http
  {{- if .Values.stableDiffusion.ingress.tls }}
  tls:
    - hosts:
        - {{ .Values.stableDiffusion.ingress.host | quote }}
      secretName: {{ .Release.Name }}-sd-tls
  {{- end }}
{{- if .Values.keda.enabled }}
---
# Internal HTTP-only ingress for bot→SD calls via KEDA interceptor.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-stable-diffusion-internal
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-apis.labels" . | nindent 4 }}
    app.kubernetes.io/component: stable-diffusion
  annotations:
    helm.sh/chart: {{ include "ai-apis.chart" . }}
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    nginx.ingress.kubernetes.io/service-upstream: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.stableDiffusion.ingress.internalHost | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-stable-diffusion
                port:
                  name: http
{{- end }}
